---
"@ascertia-integrations/docusaurus-version-sync": patch
---

Add a `--remove` mode to prune deleted release versions from generated
Docusaurus artifacts so GitHub Pages deployments can remove orphaned version
pages when a release branch is deleted.
